services:
  chatmock:
    image: ${CHATMOCK_IMAGE:-storagetime/chatmock:latest}
    container_name: ${CHATMOCK_CONTAINER_NAME:-chatmock}
    command: ["serve"]
    env_file: .env
    environment:
      - CHATGPT_LOCAL_HOME=${CHATGPT_LOCAL_HOME:-/data}
    ports:
      - "${CHATMOCK_BIND_HOST:-127.0.0.1}:${PORT:-8000}:8000"
    volumes:
      - chatmock_data:${CHATGPT_LOCAL_HOME:-/data}
      - ./prompts/bare/prompt.md:/app/prompt.md:ro
      - ./prompts/bare/prompt_gpt5_codex.md:/app/prompt_gpt5_codex.md:ro
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health').status==200 else 1)\" "]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  chatmock-clawmem:
    image: ${CHATMOCK_IMAGE:-storagetime/chatmock:latest}
    container_name: ${CHATMOCK_CLAWMEM_CONTAINER_NAME:-chatmock-clawmem}
    command: ["serve"]
    env_file: .env
    environment:
      - CHATGPT_LOCAL_HOME=${CHATGPT_LOCAL_HOME:-/data}
    ports:
      - "${CHATMOCK_CLAWMEM_BIND_HOST:-127.0.0.1}:${CHATMOCK_CLAWMEM_PORT:-8001}:8000"
    volumes:
      - chatmock_data:${CHATGPT_LOCAL_HOME:-/data}
      - ./prompts/clawmem/prompt.md:/app/prompt.md:ro
      - ./prompts/clawmem/prompt_gpt5_codex.md:/app/prompt_gpt5_codex.md:ro
    healthcheck:
      test: ["CMD-SHELL", "python -c \"import urllib.request,sys; sys.exit(0 if urllib.request.urlopen('http://127.0.0.1:8000/health').status==200 else 1)\" "]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 5s

  chatmock-login:
    image: ${CHATMOCK_IMAGE:-storagetime/chatmock:latest}
    profiles: ["login"]
    command: ["login"]
    environment:
      - CHATGPT_LOCAL_HOME=${CHATGPT_LOCAL_HOME:-/data}
      - CHATGPT_LOCAL_LOGIN_BIND=${CHATGPT_LOCAL_LOGIN_BIND:-0.0.0.0}
    volumes:
      - chatmock_data:${CHATGPT_LOCAL_HOME:-/data}
    ports:
      - "${CHATMOCK_BIND_HOST:-127.0.0.1}:${CHATMOCK_LOGIN_PORT:-1455}:1455"

volumes:
  chatmock_data:
